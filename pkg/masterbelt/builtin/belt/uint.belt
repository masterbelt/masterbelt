/// The unsigned integer types as one union: a generic constraint for any unsigned integer.
pub type unumeric = uint | uint8 | uint16 | uint32 | uint64

/// The arbitrary-precision unsigned integer: non-negative, with no fixed upper bound.
pub type uint = builtin impl {
  /// The unary + operator: the value unchanged.
  pub extern fn pos(): self

  /// The == operator.
  pub extern fn eql(other: self): bool
  /// The != operator.
  pub extern fn neq(other: self): bool

  /// The < operator.
  pub extern fn lt(other: self): bool
  /// The <= operator.
  pub extern fn lteq(other: self): bool
  /// The > operator.
  pub extern fn gt(other: self): bool
  /// The >= operator.
  pub extern fn gteq(other: self): bool

  /// The + operator: the sum.
  pub extern fn add(other: self): self
  /// The - operator: the difference.
  pub extern fn sub(other: self): self
  /// The * operator: the product.
  pub extern fn mul(other: self): self
  /// The / operator: the quotient. A zero divisor is a compile-time error.
  pub extern fn div(other: self): self
  /// The % operator: the remainder. A zero divisor is a compile-time error.
  pub extern fn rem(other: self): self
}

/// An unsigned 8-bit integer (0 to 255).
pub type uint8 = builtin impl {
  /// The unary + operator: the value unchanged.
  pub extern fn pos(): self

  /// The == operator.
  pub extern fn eql(other: self): bool
  /// The != operator.
  pub extern fn neq(other: self): bool

  /// The < operator.
  pub extern fn lt(other: self): bool
  /// The <= operator.
  pub extern fn lteq(other: self): bool
  /// The > operator.
  pub extern fn gt(other: self): bool
  /// The >= operator.
  pub extern fn gteq(other: self): bool

  /// The + operator: the sum.
  pub extern fn add(other: self): self
  /// The - operator: the difference.
  pub extern fn sub(other: self): self
  /// The * operator: the product.
  pub extern fn mul(other: self): self
  /// The / operator: the quotient. A zero divisor is a compile-time error.
  pub extern fn div(other: self): self
  /// The % operator: the remainder. A zero divisor is a compile-time error.
  pub extern fn rem(other: self): self
}

/// An unsigned 16-bit integer (0 to 65535).
pub type uint16 = builtin impl {
  /// The unary + operator: the value unchanged.
  pub extern fn pos(): self

  /// The == operator.
  pub extern fn eql(other: self): bool
  /// The != operator.
  pub extern fn neq(other: self): bool

  /// The < operator.
  pub extern fn lt(other: self): bool
  /// The <= operator.
  pub extern fn lteq(other: self): bool
  /// The > operator.
  pub extern fn gt(other: self): bool
  /// The >= operator.
  pub extern fn gteq(other: self): bool

  /// The + operator: the sum.
  pub extern fn add(other: self): self
  /// The - operator: the difference.
  pub extern fn sub(other: self): self
  /// The * operator: the product.
  pub extern fn mul(other: self): self
  /// The / operator: the quotient. A zero divisor is a compile-time error.
  pub extern fn div(other: self): self
  /// The % operator: the remainder. A zero divisor is a compile-time error.
  pub extern fn rem(other: self): self
}

/// An unsigned 32-bit integer (0 to 4294967295).
pub type uint32 = builtin impl {
  /// The unary + operator: the value unchanged.
  pub extern fn pos(): self

  /// The == operator.
  pub extern fn eql(other: self): bool
  /// The != operator.
  pub extern fn neq(other: self): bool

  /// The < operator.
  pub extern fn lt(other: self): bool
  /// The <= operator.
  pub extern fn lteq(other: self): bool
  /// The > operator.
  pub extern fn gt(other: self): bool
  /// The >= operator.
  pub extern fn gteq(other: self): bool

  /// The + operator: the sum.
  pub extern fn add(other: self): self
  /// The - operator: the difference.
  pub extern fn sub(other: self): self
  /// The * operator: the product.
  pub extern fn mul(other: self): self
  /// The / operator: the quotient. A zero divisor is a compile-time error.
  pub extern fn div(other: self): self
  /// The % operator: the remainder. A zero divisor is a compile-time error.
  pub extern fn rem(other: self): self
}

/// An unsigned 64-bit integer (0 to 18446744073709551615).
pub type uint64 = builtin impl {
  /// The unary + operator: the value unchanged.
  pub extern fn pos(): self

  /// The == operator.
  pub extern fn eql(other: self): bool
  /// The != operator.
  pub extern fn neq(other: self): bool

  /// The < operator.
  pub extern fn lt(other: self): bool
  /// The <= operator.
  pub extern fn lteq(other: self): bool
  /// The > operator.
  pub extern fn gt(other: self): bool
  /// The >= operator.
  pub extern fn gteq(other: self): bool

  /// The + operator: the sum.
  pub extern fn add(other: self): self
  /// The - operator: the difference.
  pub extern fn sub(other: self): self
  /// The * operator: the product.
  pub extern fn mul(other: self): self
  /// The / operator: the quotient. A zero divisor is a compile-time error.
  pub extern fn div(other: self): self
  /// The % operator: the remainder. A zero divisor is a compile-time error.
  pub extern fn rem(other: self): self
}
