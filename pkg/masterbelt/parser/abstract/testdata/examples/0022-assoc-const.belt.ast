File
  ConstDecl
    name "Hi"
    value (. Identifier "sbyte" Max)
  ConstDecl
    name "Lo"
    value (. Identifier "sbyte" Min)
  ConstDecl
    name "Top"
    type Level
    value (. Identifier "Level" Max)
  ConstDecl
    name "Bottom"
    type Level
    value (. Identifier "Level" Min)
  ConstDecl
    name "W"
    value (. Identifier "Bits" Width)
  ConstDecl
    name "ServerPort"
    type Port
    value IntLit "8080"
  TypeDecl
    doc "A character level, capped at 100."
    pub
    name "Level"
    body sbyte
    const "Max"
      doc "The highest level."
      pub
      value IntLit "100"
    const "Min"
      doc "The lowest level."
      pub
      value IntLit "0"
    method "increment"
      doc "The next level."
      pub
      result self
      return (call (. self add) IntLit "1")
  TypeDecl
    pub
    name "Bits"
    body int
    const "Width"
      doc "The width in bits."
      pub
      type int
      value IntLit "32"
  TypeDecl
    pub
    name "Port"
    body int
    where (call (. (call (. self gteq) IntLit "1") anan) (call (. self lteq) (. Identifier "short" Max)))
