const MaxLevel = 100
const MinLevel = 0
const Enabled = true

/// The level range is not empty.
assert MaxLevel > MinLevel
assert MinLevel == 0
assert MaxLevel - MinLevel == 100
assert Enabled && !false
assert MaxLevel > 0 && !(MinLevel > 0)
