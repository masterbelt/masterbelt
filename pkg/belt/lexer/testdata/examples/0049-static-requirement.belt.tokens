# tokens
LineComment   "// Static member requirement: an interface may require a static fn — no receiver,"
Newline       "\n"
LineComment   "// called through the type (Counter.defaultValue()) — and a type parameter bounded"
Newline       "\n"
LineComment   "// by that interface calls it through the bound (T.defaultValue()). The static call"
Newline       "\n"
LineComment   "// resolves to the requirement's result, the static twin of a method requirement"
Newline       "\n"
LineComment   "// called on a value. A type opts in by declaring a matching static fn; the"
Newline       "\n"
LineComment   "// receiver T of a static call is a type position, never consumed as a value."
Newline       "\n"
Newline       "\n"
DocComment    "/// A behaviour for anything with a default value of its own: a required static fn"
Newline       "\n"
DocComment    "/// (no receiver), called through the type. `static name(): R` is a required static"
Newline       "\n"
DocComment    "/// member, the static twin of a method requirement `name(): R`."
Newline       "\n"
Pub           "pub"
Whitespace    " "
Interface     "interface"
Whitespace    " "
Ident         "HasDefault"
Whitespace    " "
LBrace        "{"
Newline       "\n"
Whitespace    "  "
DocComment    "/// A required static fn: every HasDefault type supplies `defaultValue()`,"
Newline       "\n"
Whitespace    "  "
DocComment    "/// called through the type rather than a value."
Newline       "\n"
Whitespace    "  "
Ident         "static"
Whitespace    " "
Ident         "defaultValue"
LParen        "("
RParen        ")"
Colon         ":"
Whitespace    " "
Ident         "nint"
Newline       "\n"
RBrace        "}"
Newline       "\n"
Newline       "\n"
DocComment    "/// A counter meets the requirement with a matching static fn — the way a type opts"
Newline       "\n"
DocComment    "/// into a static-member bound."
Newline       "\n"
Pub           "pub"
Whitespace    " "
Type          "type"
Whitespace    " "
Ident         "Counter"
Whitespace    " "
Assign        "="
Whitespace    " "
LBrace        "{"
Newline       "\n"
Whitespace    "  "
Ident         "n"
Colon         ":"
Whitespace    " "
Ident         "nint"
Comma         ","
Newline       "\n"
RBrace        "}"
Whitespace    " "
Impl          "impl"
Whitespace    " "
Ident         "HasDefault"
Whitespace    " "
LBrace        "{"
Newline       "\n"
Whitespace    "  "
Pub           "pub"
Whitespace    " "
Ident         "static"
Whitespace    " "
Fn            "fn"
Whitespace    " "
Ident         "defaultValue"
LParen        "("
RParen        ")"
Colon         ":"
Whitespace    " "
Ident         "nint"
Whitespace    " "
LBrace        "{"
Newline       "\n"
Whitespace    "    "
Return        "return"
Whitespace    " "
Int           "0"
Newline       "\n"
Whitespace    "  "
RBrace        "}"
Newline       "\n"
RBrace        "}"
Newline       "\n"
Newline       "\n"
LineComment   "// Static call through the bound: `T.defaultValue()` resolves to the requirement's"
Newline       "\n"
LineComment   "// result (nint) — the static twin of reading a member off a bounded parameter. The"
Newline       "\n"
LineComment   "// parameter T is the receiver of a static call, a type position, not a value."
Newline       "\n"
DocComment    "/// Read a bounded parameter's default through its static requirement — resolved"
Newline       "\n"
DocComment    "/// through the bound, the static twin of `v.method()` on a value of type T."
Newline       "\n"
Pub           "pub"
Whitespace    " "
Fn            "fn"
Whitespace    " "
Ident         "startValue"
Lt            "<"
Ident         "T"
Colon         ":"
Whitespace    " "
Ident         "HasDefault"
Gt            ">"
LParen        "("
RParen        ")"
Colon         ":"
Whitespace    " "
Ident         "nint"
Whitespace    " "
LBrace        "{"
Newline       "\n"
Whitespace    "  "
Return        "return"
Whitespace    " "
Ident         "T"
Dot           "."
Ident         "defaultValue"
LParen        "("
RParen        ")"
Newline       "\n"
RBrace        "}"
Newline       "\n"
EOF           ""
# diagnostics
