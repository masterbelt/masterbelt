// The exporter: two public types and two public constants.

/// A 1-D coordinate.
pub type Point = int
/// A segment between two coordinates — a distinct nominal type.
pub type Line = { from: int, to: int }
/// The zero point every path starts from.
pub const Origin = 0
/// One step along the axis.
pub const Unit = 1
