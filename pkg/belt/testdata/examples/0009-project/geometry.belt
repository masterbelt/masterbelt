// The imported module: only pub declarations are visible across files.

/// The zero point every path starts from.
pub const Origin = 0
/// A 1-D coordinate.
pub type Point = int

// Not exported — main.belt must not be able to use this.
const hidden = 1
